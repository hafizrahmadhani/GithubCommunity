<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.hafizrahmadhani.consumerapp">

    <uses-permission android:name="com.hafizrahmadhani.github.READ_DATABASE" />
    <uses-permission android:name="com.hafizrahmadhani.github.WRITE_DATABASE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <queries>
        <package android:name="com.hafizrahmadhani.github" />
    </queries>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Github">
        <activity android:name=".activity.MainActivity"
            android:label="Consumer App"></activity>
        <activity android:name=".activity.SplashScreenActivity"
            android:theme="@style/NoAction">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>